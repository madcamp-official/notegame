# Claude Design Art

This directory contains artwork authored with Claude Design. It is kept separate
from the project's other character and pet art so its source remains explicit.

The `NPC` directory contains character sprite sheets, face sets, and separate
animation strips together with their original Unity import metadata.
