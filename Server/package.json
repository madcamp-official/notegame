{
  "name": "keyboard-wanderer-server",
  "version": "1.0.0",
  "private": true,
  "description": "Authoritative Codria v4 game server for Ninja Adventure",
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "start": "node --env-file=.env src/index.js",
    "dev": "node --env-file=.env --watch src/index.js",
    "test": "node --test --test-reporter=spec",
    "world:generate": "node scripts/generate-world.js",
    "world:validate": "node scripts/generate-world.js --count 32 --summary-only"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
